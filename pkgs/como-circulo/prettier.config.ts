import { type Config } from 'prettier';

/**
 * @see https://prettier.io/docs/configuration
 */
const config: Config = {
  printWidth: 120,
  singleQuote: true,
  trailingComma: 'none',
  bracketSpacing: true,
  tabWidth: 2
};

export default config;
